# Repair System — система управления ремонтом оборудования


Система автоматизирует прием заявок, распределение работ между мастерами, отслеживание статусов и контроль качества выполненных работ.

## 🏗️ Структура проекта (монорепозиторий)

```
repair-system (root)
├── repair-system-backend/      # NestJS API (порт 3001)
│   ├── src/
│   │   ├── auth/               # JWT аутентификация
│   │   ├── common/             # Guards, декораторы, утилиты
│   │   ├── entities/           # TypeORM сущности (Users, Requests, Feedback, DeviceTypes)
│   │   ├── requests/           # Модуль управления заявками
│   │   │   ├── dto/            # DTO для create/update
│   │   │   ├── requests.controller.ts
│   │   │   ├── requests.service.ts
│   │   │   └── requests.module.ts
│   │   ├── users/              # Управление пользователями
│   │   ├── statistics/         # Аналитика и статистика
│   │   ├── app.module.ts
│   │   ├── main.ts
│   │   ├── database.ts         # Подключение БД
│   │   └── data-source.ts      # TypeORM DataSource
│   ├── package.json            # NestJS зависимости
│   ├── tsconfig.json
│   └── .env (создай сам)
│
├── repair-system-frontend/     # React + Vite (порт 5173)
│   ├── src/
│   │   ├── components/         # React компоненты
│   │   ├── pages/              # Страницы приложения
│   │   ├── hooks/              # Кастомные хуки
│   │   ├── store/              # Zustand хранилище состояния
│   │   ├── api/                # Axios клиент для API
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── package.json            # React зависимости
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── .env.local (создай сам)
│
├── Отчеты2/                    # Документация (отчёты)
├── .gitignore
└── README.md (этот файл)
```

## 🚀 Быстрый старт

### Предварительно
- Node.js 18+
- npm или yarn
- PostgreSQL 13+ (для БД)

### 1️⃣ Запуск Backend (NestJS)

```bash
cd repair-system-backend
npm install
```

Создай файл `.env`:
```env
# Server
PORT=3001
NODE_ENV=development

# JWT
JWT_SECRET=your_secret_key_change_me
JWT_EXPIRES_IN=7d

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=repair_system
DB_USER=postgres
DB_PASSWORD=postgres
DB_SYNCHRONIZE=true

# Прочее
LOG_LEVEL=debug
```

Запусти сервер:
```bash
npm run start:dev
```

Backend доступен на **http://localhost:3001**

Если видишь ошибку `Cannot find module 'qrcode'`:
```bash
npm install qrcode
npm install -D @types/qrcode
```

### 2️⃣ Запуск Frontend (React + Vite)

```bash
cd repair-system-frontend
npm install
```

Создай файл `.env.local`:
```env
VITE_API_URL=http://localhost:3001
```

Запусти dev-сервер:
```bash
npm run dev
```

Frontend доступен на **http://localhost:5173**

---

## 📦 Технологический стек

### Backend
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **NestJS** | 11 | Backend фреймворк (TypeScript) |
| **TypeORM** | ^0.3 | ORM для работы с БД |
| **PostgreSQL** | 13+ | Реляционная база данных |
| **@nestjs/jwt** | ^11 | JWT аутентификация |
| **passport-jwt** | ^4 | Passport стратегия для JWT |
| **qrcode** | ^1 | Генерация QR-кодов |
| **class-validator** | ^0.14 | Валидация DTO |
| **@nestjs/swagger** | ^7 | API документация (опционально) |

### Frontend
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **React** | 19 | UI фреймворк |
| **TypeScript** | ^5 | Типизация |
| **Vite** | ^5 | Build tool и dev server |
| **React Router** | ^6 | Маршрутизация |
| **Axios** | ^1 | HTTP клиент |
| **Zustand** | ^4 | State management |
| **Material-UI (MUI)** | ^6 | UI компоненты |
| **react-hook-form** | ^7 | Управление формами |
| **react-qr-code** | ^1 | Отображение QR-кодов |
| **Emotion** | ^11 | CSS-in-JS (используется MUI) |

---

## 👥 Роли и права доступа

| Роль | Может видеть | Может делать |
|------|---|---|
| **Admin** 👨‍💼 | Всё | Управление пользователями, удаление, изменение прав |
| **QualityManager** 📊 | Все заявки, статистика | Менять статусы, назначать мастеров, фильтровать |
| **Master** 👨‍🔧 | Назначенные заявки | Просматривать детали, сканировать QR, завершать работу |
| **Client** 👤 | Свои заявки | Создавать, редактировать, оставлять отзывы (1-5 звёзд) |

---

## 📋 Основной функционал

### Для всех пользователей
- ✅ Регистрация и вход (JWT)
- ✅ Просмотр профиля
- ✅ Просмотр отзывов
- ✅ Выход из системы

### Для Клиента
- ✅ Создание новой заявки (описание, приоритет, тип устройства)
- ✅ Редактирование неавершённой заявки
- ✅ Удаление новой заявки
- ✅ Просмотр списка своих заявок
- ✅ Оставление отзывов (рейтинг 1-5 + комментарий)

### Для Мастера
- ✅ Просмотр назначенных заявок
- ✅ Сканирование QR-кода заявки
- ✅ Отметить заявку как завершённую

### Для Менеджера по качеству (QualityManager)
- ✅ Просмотр всех заявок
- ✅ Фильтрация по статусу (Новая / В процессе / Завершена / Отклонена)
- ✅ Фильтрация по приоритету
- ✅ Изменение статуса заявки
- ✅ Назначение мастера на заявку
- ✅ Просмотр статистики:
  - Количество новых заявок
  - Заявок в процессе
  - Завершённых
  - Отклонённых
  - Средняя оценка клиентов

### Для Администратора
- ✅ Все права Менеджера
- ✅ Управление пользователями (создание, удаление)
- ✅ Полный доступ ко всем данным системы

---

## 🔄 Жизненный цикл заявки

```
1. НОВАЯ (статус: 'Новая')
   └─ Клиент создал заявку
   
2. В ПРОЦЕССЕ (статус: 'В процессе')
   └─ Admin/Менеджер назначили мастера
   └─ Мастер работает с заявкой
   
3. ЗАВЕРШЕНА (статус: 'Завершена')
   └─ Мастер отметил как готово
   └─ Клиент может оставить отзыв
   
ИЛИ

2. ОТКЛОНЕНА (статус: 'Отклонена')
   └─ Admin/Менеджер отклонили заявку
```

---

## 🛢️ База данных (PostgreSQL)

### Таблицы (сущности)

**Users** — пользователи системы
```sql
id (UUID) | name | email | password_hash | role | created_at | updated_at
```

**Requests** — заявки на ремонт
```sql
id (UUID) | client_id | title | description | status | priority | assigned_master_id
| device_type | created_at | updated_at | deadline
```

**Feedback** — отзывы на заявки
```sql
id (UUID) | request_id | rating (1-5) | comment | created_at
```

**DeviceTypes** — типы оборудования
```sql
id (UUID) | name | description
```

### Миграции

```bash
# Сгенерировать миграцию
npm run migration:generate -- -n MigrationName

# Запустить миграции
npm run migration:run
# Откатить последнюю миграцию
npm run migration:revert
```

---

## 🔌 API Endpoints (примеры)

### Authentication
```
POST   /api/auth/register          — регистрация (email, password, name, role)
POST   /api/auth/login             — вход (email, password) → JWT токен
POST   /api/auth/logout            — выход
GET    /api/auth/me                — текущий пользователь (требует JWT)
```

### Repair Requests
```
GET    /api/repair-requests        — получить заявки (с фильтром по статусу/приоритету)
POST   /api/repair-requests        — создать заявку (Client)
GET    /api/repair-requests/:id    — получить деталь заявки
PATCH  /api/repair-requests/:id    — обновить заявку (статус, мастер, данные)
DELETE /api/repair-requests/:id    — удалить заявку (только новую)

GET    /api/repair-requests/:id/qr — получить QR-код заявки
```

### Feedback (Отзывы)
```
GET    /api/feedback               — все отзывы
POST   /api/feedback               — создать отзыв (rating, comment, request_id)
GET    /api/feedback/request/:id   — отзывы по заявке
```

### Statistics
```
GET    /api/statistics             — общая статистика (для менеджера)
GET    /api/statistics/dashboard   — данные для дашборда
```

### Users (Admin only)
```
GET    /api/users                  — список пользователей
POST   /api/users                  — создать пользователя
GET    /api/users/:id              — получить пользователя
PATCH  /api/users/:id              — обновить пользователя
DELETE /api/users/:id              — удалить пользователя
```

---

## 🧪 Тестирование (предполагаемые сценарии)

### Критические сценарии ✅
1. ✅ Регистрация нового пользователя
2. ✅ Вход в систему с JWT
3. ✅ Создание заявки (Client)
4. ✅ Назначение мастера (Admin/QualityManager)
5. ✅ Изменение статуса заявки
6. ✅ Генерация и сканирование QR-кода
7. ✅ Оставление отзыва (Client)
8. ✅ Расчёт статистики

```bash
# Запуск тестов (если настроены)
npm run test
npm run test:e2e
```

---

## 🛠️ Разработка и отладка

### Backend — документация API (Swagger)
Если подключен @nestjs/swagger:
```
http://localhost:3001/api/docs
```

### Frontend — React DevTools
Установи расширение в Chrome/Firefox:
- React Developer Tools
- Redux DevTools (если используется Redux)

### Database — pgAdmin (опционально)
```bash
docker run -p 5050:80 dpage/pgadmin4
# http://localhost:5050
```

---

## 🐛 Типичные проблемы и решения

### Backend не запускается
```bash
# 1. Проверь .env файл (все переменные на месте)
# 2. Проверь подключение к PostgreSQL
# 3. Запусти миграции
npm run migration:run

# 4. Если не помогло — чистый сброс
rm -rf node_modules package-lock.json
npm install
npm run start:dev
```

### Ошибка: `Cannot find module 'qrcode'`
```bash
npm install qrcode @types/qrcode
```

### Frontend не подключается к API
```bash
# Проверь:
# 1. Backend запущен на http://localhost:3001
# 2. .env.local в frontend содержит VITE_API_URL=http://localhost:3001
# 3. CORS настроен в backend (должен быть настроен в main.ts)
```

### Ошибки при загрузке страницы в браузере
```bash
# Очистить кэш
# Ctrl+Shift+Delete (или Cmd+Shift+Delete на Mac)
# Выбрать "All time" и "Cached images and files"
# Затем Ctrl+F5 (или Cmd+Shift+R)
```

### localStorage не работает
- Проверь, не включен ли приватный режим браузера
- Проверь настройки приватности браузера (localStorage может быть отключен)

---

## 📊 Сборка для production

### Backend
```bash
cd repair-system-backend

# Сборка
npm run build

# Запуск
npm run start:prod
```

### Frontend
```bash
cd repair-system-frontend

# Сборка
npm run build

# Локальный preview
npm run preview
```

Собранные файлы:
- Backend: `dist/` папка (выполняемый код)
- Frontend: `dist/` папка (статические файлы для веб-сервера)

---

## 📝 Документация

В папке `Отчеты2/` находятся подробные отчёты:
- **Отчет_Задание_1.md** — анализ, алгоритмизация, реализация, тестирование
- **Отчет_Задание_2.md** — проектирование БД, ER-диаграммы, SQL
- **Отчет_Задание_3.md** — модификации, QR-коды, руководство, рекомендации

---

## 📜 Лицензия

Проект создан в образовательных целях для практической работы.

---

## 🔗 Полезные ссылки

- [NestJS документация](https://docs.nestjs.com)
- [React документация](https://react.dev)
- [TypeScript документация](https://www.typescriptlang.org/docs)
- [TypeORM документация](https://typeorm.io)
- [Material-UI (MUI)](https://mui.com)
- [Zustand](https://github.com/pmndrs/zustand)

---

## 👨‍💻 Автор

**GitHub:** [DOGA3228/2](https://github.com/DOGA3228/2)

---

**Версия:** 1.0.0  
**Дата:** Декабрь 2025  
**Статус:** ✅ Готово к защите
